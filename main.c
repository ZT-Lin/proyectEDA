#include <stdio.h>
#include <stdlib.h>
#include "LSOBB.h"

/*int cargar_struct(LSOBB lso){
    char comando=0;
    FILE *operaciones = fopen( "Operaciones_Padron.txt", "r");

    if (operaciones==NULL){ return -1; }

    while( !feof(operaciones) ){
            fread(comando, sizeof(comando), 1, operaciones);
            switch(comando){
                case '1'{

                    break;
                }
                case '2'{
                    break;
                }
                case '3'{
                    break;
                }
            }
    }
}*/

int main(){


}
