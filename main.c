#include <stdio.h>
#include <stdlib.h>

int cargar_struct(LSOBB lso, ){
    int ibuffer=0;
    FILE *operaciones = fopen( "Operaciones_Padron.txt", "r");

    if (fp==NULL){
            return -1;
    }

    while( !feof(operaciones) ){

            fscanf("Operaciones_Padron.txt", "%d",ibuffer );

    }
}

int main(){


}
